const path = require('path');

module.exports = {
    'config': path.resolve('src/db-migrations', 'config', 'config.json'),
    'migrations-path': path.resolve('src/db-migrations', 'migrations'),
    'models-path': path.resolve('src/api/methods', 'models')
}